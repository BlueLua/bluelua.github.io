---
description:
  "Entry point that exposes all modules under one 💤 lazily loaded table."
---

# `mods`

Entry point that exposes all modules under one 💤 lazily loaded table.

## Fields

<a id="stringify"></a>

### `stringify` (`string`)

Value: `"stringify"`

<a id="template"></a>

### `template` (`string`)

Value: `"template"`
